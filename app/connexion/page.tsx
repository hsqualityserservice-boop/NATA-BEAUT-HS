'use client'
import { FormEvent, useState } from 'react'
import Link from 'next/link'
import { authClient } from '@/lib/auth-client'
import { ArrowLeft, ArrowRight, LockKeyhole } from 'lucide-react'
export default function Connexion(){const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);const r=await authClient.signIn.email({email:String(f.get('email')),password:String(f.get('password'))});if(r.error)setError('Identifiants incorrects.');else window.location.href='/espace-client';setLoading(false)}return <main className="auth-page"><Link href="/" className="back"><ArrowLeft size={16}/> Retour au site</Link><div className="auth-card"><div className="eyebrow"><LockKeyhole size={14}/> ESPACE PRIVÉ</div><h1>Bienvenue<br/><em>chez vous.</em></h1><p>Connectez-vous pour retrouver vos factures et documents.</p><form onSubmit={submit}><label>Email<input name="email" type="email" required placeholder="vous@exemple.com"/></label><label>Mot de passe<input name="password" type="password" required placeholder="••••••••"/></label>{error&&<div className="error">{error}</div>}<button className="button button-gold" disabled={loading}>{loading?'Connexion…':'Se connecter'} <ArrowRight size={16}/></button></form><small>Besoin d’un accès ? Contactez Mery Key.</small></div></main>}
